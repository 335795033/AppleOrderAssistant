# 2026-09-04

## iPhoneOrder 扩展 v2.6：自提模式 UI 驱动修复 + 激活码验证
- **背景**：用户测送货模式期间自提模式回归：有可取货门店也不点「继续填写取货详情」；且 v2.x 的 API 轮询丢了老版本"切地区刷库存"行为（无可取货门店时反复切换省市区刷新列表）。
- **自提修复**：
  - 新建 `app/scripts/content/doPickup.ts`：`doPickupFulfillment` UI 驱动——扫门店卡片（.rf-hcard/[class*=hcard]/store radio 容器），文案含"可取货/有货"且排除"不可取货/暂无货/缺货"判为可取货；先查 `getSelectedStoreInUI` 已选中→直接点「继续填写取货详情」；无可取货→`storeSearchInPage({force:true})` 强制重选区划刷新列表，最多 8 轮；失败退回 `goOrderSteps` API 兜底。
  - `isSegmentedSelected` 从 doFroApplePages 私有挪到 `shared/util.ts`（注意：同文件多编辑并行会丢，需串行）。
  - `getStoreCanPickInfo.ts` 的 `storeSearchInPage` 加 `export` + `force` 参数（force 时跳过"区划已正确不重复点"去重）。
  - doFroApplePages 自提分支不再直接点继续/走老逻辑，改调 `doPickupFulfillment`，失败才 API。
- **激活码验证**（自用分发，防到期后白嫖）：
  - 格式 `IPO-<YYYYMMDD>-<7位大写base36>`，djb2(SECRET|expiry)；SECRET=`iPhoneOrder@2026@WangLT@Activation`（constants `ACTIVATION_SECRET`，join 混淆）。
  - 新 `app/shared/activation.ts`：`generateActivationCode`/`verifyActivationCode`（到期日当天 23:59:59 有效）。
  - popup：开启(开关=on)时校验——storage `activationValidUntil` 未过期直接过；否则 prompt 输入激活码，通过则存 validUntil 并提示到期日；弹窗开关下显示激活状态行（popup 高度 h-[15rem]→auto）。
  - content-script `doFroApplePages` 双保险：orderEnabled 后校验 activationValidUntil，未激活/过期 warn 并 return。
  - 生成器 `activation-generator.html`（项目根目录，离线打开选日期生成+复制），不进扩展包。
- manifest 2.6，构建/类型检查/产物验证（cs 含 doPickup 中文日志 + 激活文案，popup chunk 含激活 UI）/zip 78 文件全部通过。交付 `iPhoneOrder_extension_v2.6.zip`。
- 构建命令同前：`rm -rf .next extension/dist && webpack --config extension.webpack.config.js && BUILD_TYPE=extension next build && node buildAfter.js`（dangerouslyDisableSandbox）；打包 python zipfile 遍历 extension/（勿排除 dist）。

## v2.7（2026-09-04 下午）
- 激活码算法升级：djb2(32位) → SHA-256(纯JS同步实现) + 8位随机盐，格式 IPO-YYYYMMDD-<SALT8>-<32位hex签名>；新 SECRET 见 constants.ts ACTIVATION_SECRET（与 activation-generator.html 一致）。旧版激活码全部失效。sha256Hex 已与 Node crypto 对拍通过。
- 品牌替换：manifest name/description/default_title、layout.tsx metadata、全部控制台日志前缀 [iPhoneOrder]→[三丈apple助手]（50处）、生成器标题。内部变量名（iPhoneOrderConfig 等）未动。
- 图标：用用户提供的「火冒三丈」手绘图（190x194 补白成 194 方图）重新生成 extension/icons 全套（16/19/32/38/48/128）+ app/favicon.ico + public/icon_original.png。
- 版本 2.7，构建（webpack+next build+buildAfter）通过，打包 SanZhangApple_v2.7.zip。

## v2.8（2026-09-04）
- 配置页顶部新增可折叠「使用说明」区块（快速上手/两种模式/激活码/常见问题与提醒/抢到后提醒 5 节），默认展开，state helpOpen 控制。
- 版本 2.8，重建并打包 SanZhangApple_v2.8.zip。

## v2.9（2026-09-04）
- 使用说明文案修订：①快速上手改为"保存→到点后手动加入购物袋（扩展不代抢加购）→停购物袋页开启自动抢购→自动结账"；②按用户要求整节删除"激活码"说明并移除其余激活码叙述；③编号重排为 一快速上手/二两种模式/三常见问题/四抢到后提醒。
- 版本 2.9，重建并打包 SanZhangApple_v2.9.zip（78 文件）。

## v2.9 补充：一键打包脚本 build-extension.sh
- 项目根目录新增 build-extension.sh：一条命令完成 清理→webpack编译content→next build静态页→buildAfter→zip 全流程；自动读取 manifest 版本命名 SanZhangApple_vX.Y.zip；支持 --no-clean / --no-zip。已实测跑通（含完整 zip）。
- 打包本质三步：webpack 编译 content/inject → BUILD_TYPE=extension next build 出 dist → buildAfter.js 收尾；zip 时保留 extension/ 内相对结构（不含顶层文件夹）。
